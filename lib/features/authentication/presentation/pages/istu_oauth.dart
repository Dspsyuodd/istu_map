import 'package:flutter/material.dart';

class IstuOauth extends StatelessWidget {
  const IstuOauth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
